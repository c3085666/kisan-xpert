import { DatetimeDifferencePipe } from './datetime-difference.pipe';

describe('DatetimeDifferencePipe', () => {
  it('create an instance', () => {
    const pipe = new DatetimeDifferencePipe();
    expect(pipe).toBeTruthy();
  });
});
